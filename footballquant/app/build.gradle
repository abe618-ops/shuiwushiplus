plugins { id 'com.android.application' }

android {
    namespace 'com.quant.football'
    compileSdk 36
    defaultConfig {
        applicationId 'com.quant.football.fusion.v3'
        minSdk 26
        targetSdk 36
        versionCode 30
        versionName '3.0.0-football-fusion'
    }
    buildTypes { release { minifyEnabled false } }
}
