plugins { id 'com.android.application' }

android {
    namespace 'com.quant.football'
    compileSdk 36
    defaultConfig {
        applicationId 'com.quant.football.fusion.v4.multiconsensus'
        minSdk 26
        targetSdk 36
        versionCode 41
        versionName '4.0.0-multiconsensus'
    }
    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled false
        }
    }
}
